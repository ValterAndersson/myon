# Cloud Run Worker dependencies
# =============================
# This file lists ONLY what the Cloud Run worker needs.
# google-adk is NOT included — it's only for Agent Engine.
#
# For Agent Engine deployment, use agent_engine_requirements.txt instead.
#
# Versions pinned to ~= (compatible release) to allow patch updates
# while preventing breaking minor/major bumps. Last resolved: 2026-02-21.

google-cloud-firestore~=2.23.0
google-cloud-aiplatform~=1.138.0
google-cloud-logging~=3.13.0
google-cloud-storage~=3.9.0
google-auth~=2.48.0
google-genai~=1.64.0
pydantic~=2.12.0
requests~=2.32.0
python-dotenv~=1.2.0
click~=8.3.0
